containers:
  publish-container:
    environment: production
    build:
      from: ubuntu-latest
      run:
        - yarn install
      publish:
        cdn:
          artifacts:
            - path: 'assets'
              patterns: ['*.html', '**/*.js']

steps:
  - build: publish-container
